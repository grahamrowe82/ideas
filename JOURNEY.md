# JOURNEY.md — ideas.phasetransitions.ai

The pinned walkthrough for the ideas site (v1: the homepage). dogfood derives the steps file
from this. Selectors (to be honoured in the build): `#hero`, `#subscribe` (primary CTA),
`#ways-in` (the doors), `#start-here` (curated cards), `#topics`, `#arc`, `#practice`, and
`a.out` for outbound links to Substack.

## Journey: a curious stranger finds a way in

Start at: `/`

1. Land on the page. **Verify:** the hero shows the tagline ("Maps are finished. Navigation
   begins."), the one-line description, the order-from-noise signature, and a visible Subscribe
   action. It reads as a real body of thinking, not a thin landing page.
2. Read down to **ways in** (`#ways-in`). **Verify:** the doors are present and legible (by a
   feeling, by your level, by topic, by lineage, along the arc), each a clear entry point.
3. Look at **start here** (`#start-here`). **Verify:** the curated cards name real reader
   situations (new to AI, you run a team, you build with it), each pointing to a specific piece.
4. Click a curated card and a topic link. **Verify:** each `a.out` points to
   `phasetransitionsai.substack.com` with a well-formed `/p/...` slug. The funnel works: the
   site frames, then sends the reader onward.
5. Scroll through the **topic index** (`#topics`, the eight topics), the **arc** (`#arc`, three
   phases), and the **practice** link (`#practice`). **Verify:** everything renders; the "work
   with me" practice surface is present but clearly secondary, never the hero.
6. Use the **Subscribe** CTA (`#subscribe`). **Verify:** it links to the Substack.

## Responsive pass

7. Resize to 390 x 844. **Verify:** no horizontal scroll; the hero and the dot field adapt; the
   doors and the topic index stack to one column.

## Scope note for the antislop (copy) pass

All visible copy on this site is original framing, written in the Phase Transitions voice. There
is no demonstration content, so the antislop pass applies to the whole page: it should come back
clean (zero dash hits, minimal phrase hits). A failure here is a real defect in the copy.
